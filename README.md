# Hello World

This small project contains a simple Python script that prints "Hello, world!".

To run locally:

```bash
python3 hello.py
```

Git setup done for you in this session. To push to a remote, add a remote and run:

```bash
git remote add origin <your-remote-url>
git push -u origin main
```
